@model List<Category>
<div>
    <h1>Categories</h1>
    <a class="btn btn-primary" asp-controller="Category" asp-action="Create">Add Category</a>
    <table class="table">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Name</th>
                <th scope="col">Status</th>
                <th scope="col">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (Category category in Model)
            {
                <tr>
                    <th scope="row">@category.Id</th>
                    <td>@category.Name</td>
                    <td>
                        @if (!category.IsDeleted)
                        {
                            <p class="text-success">Active</p>
                        }
                        else
                        {
                            <p class="text-danger">Inactive</p>

                        }
                    </td>
                    <td>
                        <a class="btn btn-warning" asp-controller="Category" asp-action="Update" asp-route-id="@category.Id">Update</a>
                    </td>
                    <td>
                        @if (!category.IsDeleted)
                        {
                            <form asp-controller="Category" asp-action="Delete" asp-route-id="@category.Id" method="post">
                                <button class="btn btn-danger">Delete</button>
                            </form>
                        }
                        else
                        {
                            <form asp-controller="Category" asp-action="Restore" asp-route-id="@category.Id" method="post">
                                <button class="btn btn-success">Restore</button>
                            </form>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
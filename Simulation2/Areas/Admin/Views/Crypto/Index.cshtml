@model List<Crypto>
<div>
    <h1>Cryptos</h1>
    <a asp-controller="Crypto" asp-action="Create" class="btn btn-primary">Add Crypto</a>
    <table class="table">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Name</th>
                <th scope="col">Price</th>
                <th scope="col">Description</th>
                <th scope="col">Category</th>
                <th scope="col">Image</th>
                <th scope="col">Status</th>
                <th scope="col">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (Crypto crypto in Model)
            {
                <tr>
                    <th scope="row">@crypto.Id</th>
                    <td>@crypto.Name</td>
                    <td>@crypto.Price</td>
                    <td>@crypto.Description</td>
                    <td>
                        @if (crypto.Category != null)
                        {
                            @crypto.Category.Name
                        }
                    </td>
                    <td>
                        <img height="50px" width="50px" src="/uploads/@crypto.ImageUrl"/>
                    </td>
                    <td>
                        @if (!crypto.IsDeleted)
                        {
                            <p class="text-success">Active</p>
                        }
                        else
                        {
                            <p class="text-danger">Inactive</p>
                        }
                    </td>
                    <td>
                        <a asp-controller="Crypto" asp-action="Update" asp-route-id="@crypto.Id" class="btn btn-warning">Update</a>
                    </td>
                    <td>
                        @if (!crypto.IsDeleted)
                        {
                           <form asp-controller="Crypto" asp-action="Delete" asp-route-id="@crypto.Id" method="post">
                               <button class="btn btn-danger">Delete</button>
                           </form>
                        }
                        else
                        {
                            <form asp-controller="Crypto" asp-action="Restore" asp-route-id="@crypto.Id" method="post">
                                <button class="btn btn-success">Restore</button>
                            </form>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>